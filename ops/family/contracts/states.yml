# 공통 상태 패널이 사용자에게 알려야 할 내용과 행동을 정의한다.
version: 1.0.0
states: [loading, refreshing, empty, no-results, offline, stale, partial, permission-denied, unsupported, error, maintenance, update-ready]
required_content: [plain-language-title, data-preservation, available-action, retry-or-fallback, last-success-at]
forbidden: [blank-screen, infinite-spinner, existing-data-clear, raw-error-only, repeated-toast]
